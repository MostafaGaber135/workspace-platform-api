# DeveloperOS web environment (Phase 1).
# Copy to `apps/web/.env.local` and adjust. Only NEXT_PUBLIC_* variables are
# exposed to the browser.

# Base URL of the DeveloperOS API.
NEXT_PUBLIC_API_URL=http://localhost:4000
